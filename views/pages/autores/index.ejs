<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('../../partials/head'); %>
</head>
<body>

<header>
  <%- include('../../partials/header'); %>
</header>

<main>
    <div class="container" style="padding-top: 20px;">
      <div style="display: flex; justify-content: space-between; margin-bottom: 10px;">
        <h3>Autores</h3>
        <a href="autores/agregar" class="btn btn-success btn" role="button" aria-disabled="true">Agregar</a>
      </div>
        <table class="table">
            <thead>
              <tr>
                <th scope="col-4">#</th>
                <th scope="col-4">Nombre</th>
                <th scope="col-4">Acciones</th>
              </tr>
            </thead>
            <tbody>
                <% 
                    let contador = 1;

                    autores.forEach(autor => { %>
                    <tr>
                        <td><%= contador++ %></td>
                        <td><%= autor.nombre %></td>
                        <td>
                            <a href="autores/<%= autor._id %>/actualizar" type="button" class="btn btn-info">Actualizar</button>
                            <a href="autores/<%= autor._id %>/eliminar" class="btn btn-danger">Eliminar</button>
                              <a href="autores/<%= autor._id %>" class="btn btn-warning">Detalle</button>
                        </td>                        
              </tr>
               <% }); %> 
              <tr>
            </tbody>
          </table>
    </div>
</main>
</body>
</html>